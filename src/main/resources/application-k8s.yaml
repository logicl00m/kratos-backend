spring:
  jpa:
    properties:
      hibernate:
        default_schema: kratos
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${auth.oauth2.resource-server}/realms/${auth.oauth2.realm}
